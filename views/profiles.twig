<!DOCTYPE html>

<html lang="en">
{% include 'head.twig' %}
<body>
    {% include 'header.twig' %}

    <div class="content">
        <ul class="crumbs">
            <li><a href="index.php">Index</a></li>
        </ul>

        {% if system_messages %}
            <h3>System Messages</h3>
            {% for system_message in system_messages %}
                <p>{{ system_message.message }}</p>
            {% endfor %}
        {% endif %}

        {% if user_messages %}
            <h3>User Messages</h3>
            {% for user_message in user_messages %}
                <p>{{ user_message.message }}</p>
            {% endfor %}
        {% endif %}

        <h3>User Info</h3>
        <p>Some information about your user account.</p>
        
        <ul>
            <li>Enrolled for 2FA: <strong>{% if user_info.two_factor_enrolled %}Yes{% else %}No{% endif %}</strong></li>
            <li>Account Disabled: <strong>{% if user_info.is_disabled %}Yes{% else %}No{% endif %}</strong></li>
        </ul>

        <h3>Profiles</h3>
        <p>This is a list of profiles for this instance. Choose a profile to see the client configuration.</p>

        <ul>
            {% for item in profile_list %}
                <li><span title="{{ item.profile_id }}"><a href="?instance_id={{ instance_id }}&profile_id={{ item.profile_id }}">{{ item.display_name }}</a> ({% if item.two_factor %}2FA{% else %}no 2FA{% endif %})</span></li>
            {% endfor %}
        </ul>

        <h3>Keypair</h3>
        <p>Generate a new keypair for this <em>instance</em>, it can be used with all profiles of this instance.</p>

        <form method="post">
            <label>Name <input type="text" name="display_name"></label>
            <input type="submit" value="Create">
        </form>
    </div>

    {% include 'footer.twig' %}
</body>
</html>
