<!DOCTYPE html>

<html lang="en">
{% include 'head.twig' %}
<body>
    {% include 'header.twig' %}
    <div class="content">
        <ul class="crumbs">
            <li><a href="index.php">Index</a></li>
        </ul>

        {% if tokenProvider %}
            <p>Your <em>home</em> service is <code>{{ tokenProvider }}</code>.</p>
            <p>Feel free to use any of the VPN services below!</p>
        {% else %}
            <p>Before using any of the available VPN services, first choose your <em>home</em> provider for which you have an account, typically in the same country as your institute.</p>
        {% endif %}

        <ul class="chooser">
            {% for instance in instanceList %}
                <li><a href="?instance_id={{ instance.base_uri }}">{{ instance.display_name }}</a></li>
            {% endfor %}
        </ul>
    </div>
    {% include 'footer.twig' %}
</body>
</html>
