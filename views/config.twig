<!DOCTYPE html>

<html lang="en">
{% include 'head.twig' %}
<body>
    {% include 'header.twig' %}

    <div class="content">
        <ul class="crumbs">
            <li><a href="index.php">Index</a></li>
            <li><a href="index.php?instance_id={{ instance_id }}">{{ instance_id }}</a></li>
        </ul>

        <h3>Profile Config</h3>
        <p>This is the client configuration <em>without</em> the keypair.</p>

        <pre>{{ profile_config }}</pre>
    </div>

    {% include 'footer.twig' %}
</body>
</html>
